# novel2drama — 图像提示词风格化指南（N2D_VISUAL_GUIDE）

> 面向 opencode 编码 agent 的**图像 prompt 构造**指南。目的是让 agent 在需要
> 生成图片（段首图、关键帧、资产图等）时，能组织出**符合品牌/风格的提示词**。
>
> **重要边界**：生图能力**不在 opencode 里**（opencode 不是生图模型）。agent 只负责
> **组织符合品牌的图片 prompt**，真正生图走 n2d 后端 image 命令（经 GPU 生图网关）。
> 所以本指南只讲「如何写 prompt」，不讲 API/端点调用。

---

## 一、通用构建契约（任何生图前必走）

### 1. 解析需求
从自然语言提取：主体、动作、场景、情绪、用途、画风/媒介、时间天气、画幅、景别、
必须保留、必须避免、是否有参考图、是直接生图还是仅交付提示词。
信息足够就直接构建；只有缺失项会**根本改变结果**时才问一个问题。

### 2. 主风格模块路由（同一任务只选一个主模块，辅助模块最多一个）
- **电影截图/叙事一帧/剧照/导演感/明确景别** → 电影叙事帧（见「三」）
- **写实照片/产品/人像/建筑/美食/棚拍/广告** → 写真照片（见「三」）
- **动漫/二次元/漫画/赛璐璐/绘本插画** → 动漫插画（见「三」）
- 国画/水墨/工笔/油画/水彩 → 传统绘画；像素画/图标/Logo/扁平 → 像素图形；3D/CG/PBR → 3D 渲染

### 3. 构建契约（主模块必须产出 4 件套）
1. **画面策略**：1–3 句，说明视觉路线、构图/空间、光色或媒介重点。
2. **主提示词**：连续、具体、可直接投喂；按该媒介最自然的语言组织（中文场景用中文）。
3. **精简负面约束**：只列 6–12 个本场景高风险问题，不机械重复主提示词。
4. **执行参数**：model / size / quality / 参考图处理。
   - 生图走 n2d 后端时，这些参数由后端/n2d 命令决定，agent 只写 prompt 即可。

### 4. 通用原则
- **抽象风格名必须翻译为可见规则**：形体、线条、色块、材质、空间、光源、焦点、笔触/渲染。
- 用户指定的景别、画幅、文字、角色特征 = **硬约束**。
- 参数只在能改变画面时用，禁止为显专业堆术语；摄影参数（Kelvin/sensor/f-stop/shutter）不是全媒介通用语言。
- 风格融合必须说明碰撞逻辑，不要只堆风格名。
- **结尾统一加质量句**（公司统一 QC）：`画面干净，通透，材质完整，自然纹理平滑，统一，主体清晰，背景层次分明，避免过度锐化色斑，噪点碎文崩坏和畸变。`

### 5. 自检
- 是否只选了一个主模块？
- 媒介是否被摄影参数污染？
- 用户硬约束是否落实为可见画面规则？
- 主提示词与负面约束是否互相打架？

---

## 二、品牌风格 Token（遇到指定品牌/系列时强制应用）

### 苑和轩（咖啡厅品牌）
- **品牌固定标识** = 「苑和轩」书法 logo + 其正下方拼音 `YUAN HE XUAN`（无衬线全大写）。
  **这是一整块固定品牌标志：不可改字/重绘/放大缩小/删减。** 生成时让 image 沿用参考图这块区域原样。
- 全图英文**仅限**该拼音，其余文案一律中文。
- **产品事实禁忌**：咖啡豆**不是**阿拉比卡、**不是**新鲜烘焙——文案禁写「阿拉比卡/新鲜烘焙/每日新鲜/百分比」等错误表述。
- **文案避坑**：英文直译成中文很生硬（如 ROASTED FRESH DAILY →「每天新鲜烘焙」= 翻译腔）。
  应拟贴合中文语境的自然文案（例：美式 / 醇香顺滑·唤醒每个清晨；经典 / 现场研磨·现点现做）。
- 印刷文字必须**印正**：水平直立、字头朝上、不倾斜/旋转/歪斜/颠倒，不被杯身弧度扭曲，汉字笔画完整。

### 八街手作（奶茶品牌海报）
- **风格锚点** = `bajie_poster_v2_a4.png`：牛皮纸暖棕复古手作 + 深棕手写标题 + 金黄竖排圆角标签。
  （同系列延续时用最近认可那张做参考，比例保持一致。）
- **宣传海报不标价格**（用户要「宣传海报」时 prompt 写「不要标价格」）。
- **季节词必须匹配当前月份**；用户给「带秋季字样」用字面「秋季」非「秋日」。
- **粉紫/可爱风克制**：粉紫色系只做点缀保留简洁，**禁止星星/云朵/爱心/蝴蝶结/表情贴纸/卡通图案**堆砌；
  风味色（如芋泥紫）只用在产品本身（挂壁/层/标签底色），背景保持暖棕中性。
- 秋日点缀「数量上限+色调约束+排除清单」：3–5 片低饱和橙红/金黄落叶，禁南瓜/稻草人/圣诞/卡通贴纸。

---

## 三、常用媒介要点

### 电影叙事帧（cinematic still）— 短剧段首图首选
- 找**正在发生/即将发生/刚结束的那一秒**，画面要暗示前一瞬和后一瞬；先立人物大小/位置/遮挡/距离/动作，再装饰环境。
- **用户指定景别 = 硬约束**。景别翻成可见构图：
  - 特写（close）：脸/肩，仅留必要环境线索。
  - 中景（medium）：腰部以上，手和关系可读。
  - 远景/大远景（wide/extreme）：主体小但动作可读，环境承载规模/孤独/压力。
- **有动机光源**（场景里存在的：窗/阴天/灯/电视/路灯/车灯/火光/星光），不要均匀打光。
- 色彩 = 一个主家族 + 一个辅家族 + 一个小点缀，不要全屏统一滤镜。
- 只选相关瑕疵：柔和高光、局部失焦、运动模糊、胶片颗粒、压缩纹理、克制锐度。
- 结尾明确这是一帧**电影叙事**，不是海报/时尚摆拍/概念艺术。
- 负面约束常含：商业广告、居中英雄海报、时尚摆拍、塑料皮肤、均匀打光、假轮廓光、全局 HDR、过度锐化、游戏 CG、浮动 UI、文字、水印。

### 写真照片（photographic）— 产品/人像/场景
- 构建顺序：主体/用途 → 场景与动作 → 布光 → 机位景别 → 镜头/景深 → 构图 → 材质 → 色彩/后期 → 摄影质控。
- 用精确参数只在有用时：主/辅/轮廓/实用光（方向、软硬、比例）、色温（混光或产品色准时）、焦距/光圈/焦平面（透视/景深时）。**别每条都填**，避免矛盾（f/16 配奶油浅景深、1/4000s 配长拖影）。
- 子路线：产品（形状可读、材质准确、反射受控、边缘干净、品牌负空间）；人像（姿态表情、皮肤质感、可信光、服饰背景关系）；美食（新鲜感、纹理、可信但诱人的色）。

### 动漫插画（anime illustration）— 漫剧/插画风格帧
- 构建顺序：角色身份与动作 → 剪影/姿态/表情 → 场景与故事节拍 → 构图 → 线条语言 → 形色设计 → 背景处理 → 打光作为色块关系 → 完成度。
- 明确：线条（干净赛璐璐线/铅笔/墨水/变宽/无线条/粗糙分镜）、形体与比例（脸/手/服装/配饰）、上色（赛璐璐色块/柔和过渡/限色/粉彩/饱和点缀/印刷网点）、背景（全绘/简化/氛围/留白）。
- **不要**默认加相机/传感器/f-stop/ISO/胶片/写实皮肤；角色一致性=不可变特征一次写清、与场景动作分开。
- 质量：连贯解剖、可读的手、稳定的面部身份、干净的遮挡、无意外文字。

---

## 四、用户偏好（涉及图像任务时遵守）
- 用户已明确：**视觉/审美审查交给用户自己**（不信任 vision 复核审美）。agent 只做本地确定性校验（尺寸/像素防坏图），不做 vision 审美复核。
- 生成类任务不再用 vision 复核文字/字形（对手写/艺术字误读率高），image2 输出即权威。
- 用户偏好 image2 直接重绘，不做本地 PIL 手工裁切抠图（除非用户明确要求）。
