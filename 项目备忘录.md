# novel2drama 项目备忘录

> 项目：小说 → 单集 AI 漫剧（15 秒段拼接）工作台。
> 开发主地在**宿主机** `/root/projects/novel2drama`（git 仓库）；**GPU 服务器** `/data/projects/novel2drama` 为运行部署地（rsync 同步，无 git）。

## 运行环境
- 宿主机：/root/projects/novel2drama，FastAPI（uvicorn），端口 8010。
- GPU 服务器：36.138.26.41 `/data/projects/novel2drama`，systemd `novel2drama.service` + `novel2drama-worker.service`。
- 依赖：fastapi / uvicorn[standard] / jinja2 / python-multipart / itsdangerous / aiofiles。
- 数据库：SQLite `data/novel2drama.db`；启动时 `schema.migrate_episodes` 幂等迁移。

## 目录结构
```
app/
  main.py         # FastAPI 入口, 路由注册, /files 文件服务
  config.py db.py models.py schema.py
  routers/        # projects/novels/storyboard/assets/generation/review/export/episodes
  services/       # llm/gateway/prompt/storage/ffmpeg/agent/system_status/config
  workers/        # gpu_worker.py
cli/n2d.py, n2d   # 直连后端 REST 的 CLI（/usr/local/bin/n2d）
templates/        # base + projects/novel_edit/storyboard_edit/asset_review/export/project_detail
static/app.css app.js
data/             # 项目数据 + db
scripts/          # init_db.py run_dev.sh
```

## 状态机（项目 status 字段）
draft → novel_ready → storyboard → assets → keyframes → h3 → export

## 分集（episodes）契约（#1 已落地）
- 一个项目多集；共享全局角色/场景资产（保留 `project_id`），各集持独立 novel_versions/segments/jobs/exports/agent_tasks/agent_patches/reviews（`episode_id`）。
- 前端当前集 `episode_id` 存 localStorage `n2d_episode_<pid>`，URL query `?episode_id=`。
- 选集切换通过 `episode-select` change 重定向到带 `episode_id` 的当前 URL。

## 勿动文件
- 无（前端正在重构中）。

## 资产生成渠道（2026-09-03 资产体系升级）
- 资产图统一走 image2（smmmc gpt-image-2，`app/services/image2_client.py`，纯 urllib）；Z-Image 仅保留段首图关键帧。
- image2 依赖 SMMMC_API_KEY（GPU 侧 /etc/novel2drama.env 只有 LLM_API_KEY，两者同值，config 已回退）。
- 画幅默认 4:3（config ASSET_DEFAULT_ASPECT），可调；角色多视角例外 16:9。
- 存储子目录：character→assets/characters、scene→assets/scenes、item→assets/items。

## 部署同步
- 宿主机改完 → rsync 到 GPU → 重启 GPU systemd。见 docs/开发进度.md 部署节。

## 权威需求档
- `/root/vault/40-项目/AI漫剧-小说转单集工作流架构-20260822.md`（架构）
- `/root/vault/40-项目/AI漫剧-小说转单集-API设计与数据模型-20260822.md`（API）
- `/root/vault/40-项目/novel2drama-需求变更排期-20260831.md`（#1~#6 变更）
- `/root/vault/40-项目/novel2drama-需求-资产体系升级-20260903.md`（资产体系升级：角色多视角/物品资产/画幅4:3，**需求分析稿未开工**）
- 本项目 `docs/需求与架构.md` = 上述 vault 档的精简搬运。
