# novel2drama 开发进度

> 每完成一项更新此处。⬜ 待开发 / 🔄 进行中 / ✅ 已完成 / 📌 待独立审查。

## 开发顺序（架构 §11）
1. ✅ 项目骨架
2. ✅ 项目管理
3. ✅ 小说版本编辑（原文展示 + 章节解析）
4. ✅ 15s 段 + 段内节拍编辑（分镜协同页，三栏）
5. ✅ Agent 分镜补全（后端已有 generate/patch/apply/reject；前端落地「AI 生成分镜」+「待应用 patch」预览/应用/拒绝，见 commit 6fef9f3）
6. ✅ 资产库
7. ✅ Z-Image 段首图（接口+worker 已接线；新增 8.5 段首图复核页：聚合接口+侧栏+生成/重抽/选定，commit 1769072）
8. ✅ H3 15s 段生成（接口+worker 已接线；新增 8.6 15s段复核页：聚合接口+侧栏+变体生成/重抽/选定，commit deb4b9b）
9. ✅ 复核与重抽（8.5 keyframe_review + 8.6 segment_review 独立复核页已实现）
10. ✅ 拼接导出（基础导出 + 段顺序调整 ↑↓ + succeeded 状态映射/概览统计一致，commit 17d217f）

## 变更需求进展
- ✅ #1 项目分集（commit 25d9c91 + 8faa261 修缓存）：episodes 表 + episode_id 分表 + 选集栏。
- ✅ #3 前端重构 · 常驻左侧 sidebar：base.html 改 sidebar+内容区骨架；含返回项目列表入口 + 当前集标题/选集器（loadSidebar/loadEpisodeBar）+ 菜单项可收起（body.sidebar-collapsed）；基于 #1 的 episode_id 上下文，菜单链接自动注入 episode_id。
- ✅ #2 项目进度栏去除（随 #3）：删除 stage-track/progress-panel，loadDetail 不再渲染进度条。
- ✅ #4 任务动态收起（随 #3）：job-list 从主视图移入 #jobs-drawer 抽屉；侧栏「任务动态」菜单项带 #jobs hash 自动展开并加载。
- ✅ #5 项目概览收起（随 #3）：概览保留在项目详情页（侧栏「项目概览」入口），移除页面 module-grid 模块入口；各子页顶部「返回项目」按钮移除（侧栏返回列表承接）。
- ✅ #6 资产上传图片：后端新增 POST /projects/{project_id}/assets/upload（multipart，存 data/projects/{id}/assets/{characters|scenes}/，上传即成为该资产选定候选图，可再点「确认」入库）；list_assets 改为 LEFT JOIN 返回 selected_image；前端资产页新增「上传本地素材」表单 + 补 loadAssets 列表渲染（缩略图/生成候选/确认）。

## 新需求（2026-08-31，见 docs/新需求-opencode接入与新建浮窗-20260831.md）
- ✅ **需求B：右侧新建栏改居中 Modal 弹窗**（commit 01008c1）：projects/novel_edit/asset_review/export 四页移除 .split-layout 右列 aside 表单，改页头 + 新建 按钮 + 居中 Modal；app.js 新增 openModal/closeModal + 提交后关闭；app.css 新增 .modal-mask/.modal。
- ✅ **需求A：opencode(ACP) agent 接入 + n2d CLI 工具**：后端核心已通（acp_bridge / n2d_cards / agent 路由 / opencode.json / N2D_GUIDE.md），端到端实测跑通；**前端「AI 助手」对话窗已完成**（commit f08032e，agent.css + agent.js + base.html 注入浮窗，版本号 `20260831m3`）。详见 docs/新需求-opencode接入与新建浮窗-20260831.md 的「需求A」小节与「未完成」清单。

## 部署
- 同步：宿主机 → GPU `/data/projects/novel2drama`（rsync 关键文件 + 重启 systemd）。
- 版本号：app.js/app.css 加 `?v=` 版本串防浏览器缓存（已用 `20260831m3`，改了记得递增）。
- ✅ 需求A 后端源码已 rsync 到 GPU + systemd novel2drama 已 restart（2026-09-01）。宿主机 commit f08032e 已提交。

## 需求A 验证状态（2026-09-01）
- ✅ 宿主机前端渲染：CDP 实测浮窗注入/展开/无 JS 报错。
- ✅ GPU 读操作端到端：SSE 完整流式（session→tool→agent_chunk→done），33.8s/146 事件。
- ✅ GPU 行为卡片判定：写命令→中文卡片（write=True），读命令自动放行（write=False）。
- ⚠️ 写操作 GUI 闭环（卡片→批准→数据变更）：组件已单独实证（卡片判定/批准路由/ACP 响应格式/opencode 联通），本轮受 SSE 同步脚本超时+browser-exec 不可用未轮内闭环，**待用户浏览器实点验收**（http://36.138.26.41:8010）。
- ⚠️ 宿主机 opencode 1.14.28 的 `opencode acp` 不产生流式事件（`run` 正常），本地调试 ACP 需升级，不影响 GPU 交付。
- ℹ️ GPU `/etc/novel2drama.env` LLM_API_KEY 实测有效（对 deepseek-v4-pro / flash-vision-exp 均 OK），与宿主机 SMMMC_API_KEY 同 key，**无需覆盖**。

## 本地测试
- `python -m uvicorn app.main:app --port 8010`（需先 `pip install -r requirements.txt`）。
- CLI：`n2d --help`（宿主机 & GPU 均已软链 /usr/local/bin/n2d）。

## 需求A 前端 5 项体验修复（commit de20980，版本号 `20260831m4`，2026-09-01）
按「乱码→现状接口→持久化/多会话→动画→视觉skill」顺序全部落地并部署 GPU。

- ✅ **⑤ 乱码/缺字**：后端 agent_chat StreamingResponse 显式 `charset=utf-8`；前端 consumeSSE 跨 chunk 完整累积 buffer + TextDecoder 持久 + 尾部冲刷；appendAssistant 累积串一次性设 textContent。GPU 实测 171 个 agent_chunk、0 个 U+FFFD、持久化文本完整无缺字。
- ✅ **② 省 token（n2d context 现状接口）**：`GET /api/v1/projects/{id}/context` 聚合项目/分集/当前集/版本/段落/资产；`n2d context <pid>` CLI；N2D_GUIDE 顶部强引导先跑 context；n2d_cards 判 context 为读命令（自动放行不弹卡）。GPU 实测 agent 首趟即调 `n2d context`。
- ✅ **③ 全后端持久化 + 多会话 + 实时刷新（禁 localStorage）**：新增 agent_sessions / agent_messages / project_version 表；/agent/sessions CRUD；acp_bridge by_project 改 by_session（UI 会话 1:1↔ACP session）+ 并发上限 5；写操作批准后 bump version + SSE 广播 data_changed；前端会话列表/新建/历史回放（从后端拉取）+ SSE 订阅 `/projects/{id}/events` + 5s 版本号轮询兜底。GPU 实测会话/历史完整回放、用户消息持久化。
- ✅ **① 工具动画**：删 `.ai-tool .sp` 死码；`.loading` 清理为仅 spinner；长条静止，仅 `.ic.spinner` 图标位旋转。
- ✅ **④ 视觉 skill → N2D_VISUAL_GUIDE.md**：由 image2-prompt-router + 各 style 模块 + 苑和轩/八街品牌 token 编译成 opencode 可读指南，已拷 GPU；`~/.config/opencode/opencode.json` 的 `instructions` 同时指向 N2D_GUIDE.md + N2D_VISUAL_GUIDE.md（绝对路径，已置顶）。生图真执行仍走 n2d 后端 image 命令。

- 版本号：`20260831m3` → `20260831m4`（base.html）。
- 部署：rsync（排除 data/.db/.git/__pycache__）→ GPU + `systemctl restart novel2drama + novel2drama-worker`（均 active，启动迁移建表成功，WAL 保持）。
- 待用户浏览器实点：写操作 GUI 闭环（卡片→批准→两端刷新）需人工在 http://36.138.26.41:8010 验证。
